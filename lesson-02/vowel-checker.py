# # Python program to check if string starts with vowel

# check if noun starts with vowel
noun = input('word: ')
is_vowel = ("not working")
if noun[0].lower() in ['a', 'e', 'i', 'o', 'u']:
    is_vowel = 'an'
else:
    is_vowel = 'a'
